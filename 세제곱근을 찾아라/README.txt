C로 풀 예정.
	- 정수 값의 반의 반의 반까지 반복문을 통해 해결하려함. -> 8에서 문제가 생김
	- 8에서 문제가 생겨서 반의 반만 하려함. -> 1에서 문제가 생김 -> 반의 반이라는 개념으로는 애매함.
	- DP처럼 배열에 미리 세제곱된 수를 넣어놓으려 함.
	- -1이 나오는 경우의 수를 대비하여 세제곱된 배열중에서 입력된 정수를 넘지 않는 애들만 비교함.
